{
  "name": "worldtracking-camerakit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "eslint-plugin-erasable-syntax-only": "^0.3.1",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "@snap/camera-kit": "^1.7.0",
    "rollup": "^4.44.0"
  }
}
